security:
  public-routes:
    - /
    - /index.html
    - /favicon.png
    - /logo1.png
    - /_app/**
    - /auth/login

# Actuator
management:
  endpoints:
    access:
      default: none

# SpringDoc Swagger
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
